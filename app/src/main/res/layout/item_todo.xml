<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- Circle checkbox — red ring unchecked, green filled checked -->
    <ImageButton
        android:id="@+id/checkCircleButton"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:background="@drawable/circle_icon_badge"
        android:src="@null"
        android:layout_marginEnd="12dp"
        android:contentDescription="Mark complete"/>

    <!-- Todo text -->
    <TextView
        android:id="@+id/todoText"
        android:layout_width="0dp"
        android:layout_height="52dp"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:background="@drawable/todo_item_bg"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:layout_marginEnd="8dp"/>

    <!-- Delete button — bigger, darker red -->
    <ImageButton
        android:id="@+id/deleteTodoButton"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/todo_delete_btn"
        android:src="@drawable/ic_delete"
        android:padding="12dp"
        android:contentDescription="Delete"/>

</LinearLayout>

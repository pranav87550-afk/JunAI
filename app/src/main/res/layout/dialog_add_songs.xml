<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/dialog_card_bg"
    android:padding="4dp">

    <!-- Title -->
    <TextView
        android:id="@+id/addSongsTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add Songs"
        android:textColor="#E53935"
        android:textSize="22sp"
        android:textStyle="bold|italic"
        android:fontFamily="sans-serif-condensed"
        android:gravity="center"
        android:paddingTop="18dp"
        android:paddingBottom="6dp"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1.2dp"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="4dp"
        android:background="#E53935"/>

    <!-- Song checklist -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/addSongsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:minHeight="120dp"
        android:maxHeight="420dp"
        android:clipToPadding="false"
        android:paddingBottom="4dp"/>

    <!-- Footer buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="16dp">

        <Button
            android:id="@+id/cancelAddSongsButton"
            android:layout_width="0dp"
            android:layout_height="46dp"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:text="CANCEL"
            android:textColor="#CCCCCC"
            android:textSize="14sp"
            android:textStyle="bold"
            android:background="@drawable/dialog_btn_dark"/>

        <Button
            android:id="@+id/confirmAddSongsButton"
            android:layout_width="0dp"
            android:layout_height="46dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="ADD"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold"
            android:background="@drawable/dialog_btn_red"/>

    </LinearLayout>

</LinearLayout>
